@model IEnumerable<Category>
@{
    ViewData["Title"] = "List";
}
<div class="container mt-3">
    <div class="row">

        <h1>Category List</h1>
        <hr />
        <div class="col">
            @if (Model.Count() > 0)
            {

            }
            else
            {
                <div class="alert alert-danger">
                    Category did not add . . .   
                </div>
            }
        </div>
    </div>
</div>

